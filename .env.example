# Docker Compose variable substitution (.env is gitignored).
# These are required to build/run the frontend container.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-public-key>

